version: '3.8'

services:
  media-sequencer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: multi-window-media-sequencer
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - SPRING_PROFILES_ACTIVE=prod
    restart: always
